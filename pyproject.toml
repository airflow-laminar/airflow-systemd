[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"

[project]
name = "airflow-systemd"
authors = [
    {name = "the airflow-systemd authors", email = "t.paine154@gmail.com"},
]
description = "Systemd operators and configuration for long-running tasks"
readme = "README.md"
license = { text = "Apache-2.0" }
version = "0.2.0"
requires-python = ">=3.11"
keywords = []

classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python",
    "Programming Language :: Python :: Implementation :: CPython",
    "Programming Language :: Python :: Implementation :: PyPy",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]

dependencies = [
    "airflow-ha>=1.4.1,<1.7",
    "airflow-pydantic>=1.5.5,<1.6",
    "systemd-pydantic>=0.1,<0.2",
]

[project.optional-dependencies]
develop = [
    "build",
    "bump-my-version",
    "check-dist",
    "codespell",
    "hatchling",
    "mdformat",
    "mdformat-tables>=1",
    "pytest",
    "pytest-cov",
    "ruff",
    "twine",
    "ty",
    "uv",
    "wheel",
    "airflow-config>=0.1.2",
    "airflow-pydantic",
    "systemd-pydantic>=0.1,<0.2",
]
airflow = [
    "apache-airflow>=2.8,<4",
    "apache-airflow-providers-ssh",
    "apache-airflow-providers-standard",
]
airflow3 = [
    "apache-airflow>=3,<3.4",
    "apache-airflow-providers-ssh",
    "apache-airflow-providers-standard",
]

[project.scripts]

[project.urls]
Repository = "https://github.com/airflow-laminar/airflow-systemd"
Homepage = "https://github.com/airflow-laminar/airflow-systemd"

[tool.bumpversion]
current_version = "0.2.0"
commit = true
tag = true
commit_args = "-s"

[[tool.bumpversion.files]]
filename = "airflow_systemd/__init__.py"
search = '__version__ = "{current_version}"'
replace = '__version__ = "{new_version}"'

[[tool.bumpversion.files]]
filename = "pyproject.toml"
search = 'version = "{current_version}"'
replace = 'version = "{new_version}"'

[tool.coverage.run]
branch = true
omit = [
    "airflow_systemd/tests/integration/",
]

[tool.coverage.report]
exclude_also = [
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "@(abc\\.)?abstractmethod",
]
ignore_errors = true
fail_under = 50

[tool.hatch.build]
artifacts = []

[tool.hatch.build.sources]
src = "/"

[tool.hatch.build.targets.sdist]
packages = [
    "airflow_systemd",
]

[tool.hatch.build.targets.wheel]
packages = [
    "airflow_systemd",
]

[tool.hatch.build.targets.wheel.shared-data]

[tool.pytest.ini_options]
addopts = [
    "-vvv",
    "--junitxml=junit.xml",
]
testpaths = "airflow_systemd/tests"

[tool.ruff]
line-length = 150

[tool.ruff.lint]
extend-select = [
    "I",
]

[tool.ruff.lint.isort]
combine-as-imports = true
default-section = "third-party"
known-first-party = [
    "airflow_systemd",
]
section-order = [
    "future",
    "standard-library",
    "third-party",
    "first-party",
    "local-folder",
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = [
    "F401",
    "F403",
]

[tool.yardang]
title = "airflow-systemd"
root = "README.md"
theme = "sphinxawesome_theme"
pages = [
    "docs/src/tutorial.md",
    "docs/src/how-to.md",
    "docs/src/explanation.md",
    "docs/src/api.md",
]
use-autoapi = false

[tool.yardang.llms]
enabled = true
description = "Apache Airflow integration for systemd-managed jobs"
build-parallel = true
suffix-mode = "auto"
full-build = true
